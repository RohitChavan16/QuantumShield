export const Placeholder = ({ title }: { title: string }) => (
  <div className="p-8 text-white h-full flex flex-col justify-center items-center opacity-50">
    <h2 className="text-2xl font-bold mb-2">{title}</h2>
    <p>Phase 3 - Implementation Pending</p>
  </div>
);
